# contikipy config
path:    /home/mike/Repos/contiki-usdn
wd:      /examples/ipv6/sdn-udp
log:     /tools/cooja/build/COOJA.testlog
out:     /home/mike/Results
csc:     usdn-20-node.csc
target:  exp5438

# log parser config
multi_sim_analysis:
  run:                  False
  cmp_tr_br:            1  # Compare traffic ratio over bit rates

# multiflow config
multiflow:
  1_flow:
    - FLOWID:         1
      TXNODE:         0
      RXNODE:         1
      BRMIN:          60
      BRMAX:          75
  2_flow:
    - FLOWID:         1
      TXNODE:         3
      RXNODE:         1
      BRMIN:          0.23
      BRMAX:          0.23
    - FLOWID:         2
      TXNODE:         3
      RXNODE:         1
      BRMIN:          10
      BRMAX:          10

# simulation config
simulations:
  # - desc:           REROUTE_RPL
  #   csc:            usdn-reroute.csc
  #   makeargs:
  #     MAC:          CONTIKIMAC
  #     SDN:          0
  #     NUM_APPS:     2
  #     APPID:        [0,1]
  #     TXNODE:       [2,3]
  #     RXNODE:       [1,1]
  #     BRMIN:        [1,20]
  #     BRMAX:        [5,20]
  #     REROUTE:      1
  #   plot:
  #     - flow_lat            # Flow vs latency
  #
  # - desc:           REROUTE_SDN
  #   csc:            usdn-reroute.csc
  #   makeargs:
  #     MAC:          CONTIKIMAC
  #     SDN:          1
  #     NUM_APPS:     2
  #     APPID:        [0,1]
  #     TXNODE:       [2,3]
  #     RXNODE:       [1,1]
  #     BRMIN:        [1,20]
  #     BRMAX:        [5,20]
  #     REROUTE:      1
  #   plot:
  #     - flow_lat            # Flow vs latency

  # - desc:           RPL_APP
  #   makeargs:
  #     MAC:          CONTIKIMAC
  #     SDN:          0
  #     NUM_APPS:     1
  #     APPID:        [1]
  #     TXNODE:       0
  #     RXNODE:       1
  #     BRMIN:        [60]
  #     BRMAX:        [75]
  #   plot:
  #     - hops_rdc            # Radio duty cycle vs hops
  #     - hops_prr            # Packet receive ratio vs hops
  #     - hops_lat_mean       # Hops vs mean latency
  #     - hops_lat_e2e        # Hops vs end-to-end latency

  - desc:           SDN_APP
    makeargs:
      MAC:          CONTIKIMAC
      SDN:          1
      NUM_APPS:     1
      APPID:        [1]
      TXNODE:       0
      RXNODE:       1
      BRMIN:        [60]
      BRMAX:        [75]
    plot:
      - hops_rdc            # Radio duty cycle vs hops
      - hops_prr            # Packet receive ratio vs hops
      - hops_lat_mean       # Hops vs mean latency
      - hops_lat_e2e        # Hops vs end-to-end latency
      - sdn_join            # SDN controller join time
      - sdn_traffic_ratio   # SDN traffic ratio

# analysis (comparison) config
analysis:
  - sims:
    - RPL_APP
    - SDN_APP
    plots:
      - hops_rdc
